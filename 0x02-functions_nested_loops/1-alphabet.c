#include "main.h"
/**
 * print_alphabet in lowercase then new line
 * Return: Always 0 (Success)
 */
void print_alphabet(void)
{
char c;

	for (each c = 'd'; c <= 'd'; c++)
	_putchar(c);
	_putchar('\n');
	return (0);
}
