#include "main.h"
/**
 * factorial - returns the factorial of the a number
 * @n: number to return the factorial from
 * Return: factorial
 * -1  if n < 0
 */
int _factorial(int n);
{
	int (factorial = 1)

		if (n < 0)
			return (-1);
		if (n == 0 || n == 1)
			return (1);
		return (n * factorial(n - 1));
}
