#ifndef MACROLIKE_H
#define MACROLIKE_H

/**
 * MACRO_LIKE - absolute value of a number x
 *
 * @x: type in the value of macro
 *
 * Return: Header file with macrolike value.
 */

#define ABS(x) ((x) < 0 ? -(x) * (x) : (x))

#endif
